# Wishlist

Authenticated customers can add/remove product variants or products from a wishlist. The UI should provide fast feedback and preserve state across devices for signed-in users.

Unavailable/archived items remain visible with appropriate messaging until removed or automatically retired according to business rules.

Wishlist actions must be ownership-protected and should not reveal another user's data.