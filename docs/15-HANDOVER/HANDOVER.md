# Handover

Handover package includes architecture, database/migrations, deployment process, environment-variable inventory, admin guide, maintenance guide, troubleshooting and ownership map.

No credentials are placed in documentation. Instead, document where the business securely stores and controls them.

A handover is complete only when a second competent developer can run, deploy and maintain the system using the repository documentation.