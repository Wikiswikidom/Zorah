# Troubleshooting

Start by identifying whether the issue is browser/UI, server, database, provider integration or deployment related.

Check deployment logs, application logs, provider dashboards and relevant audit records without exposing secrets.

Common categories: authentication failures, RLS denial, stale content, payment callback issues, email delivery, inventory mismatch, image upload problems and deployment environment variables.

Document every production incident and convert recurring issues into tests or operational checks.