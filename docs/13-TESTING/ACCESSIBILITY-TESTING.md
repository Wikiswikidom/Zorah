# Accessibility Testing

Target WCAG-aligned accessible behavior. Test keyboard navigation, focus visibility, semantic headings, labels, form errors, color contrast, alt text, reduced motion and screen-reader behavior for critical flows.

Touch targets must be practical on mobile. Modals/drawers must manage focus correctly.

Checkout and account flows receive priority because inaccessible controls can directly block customers.