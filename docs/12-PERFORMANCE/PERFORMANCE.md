# Performance

Performance budget priorities: fast first render, responsive interaction, stable layout and minimal JavaScript.

Use server rendering where useful, optimized images, responsive image sizes, modern formats, lazy loading for below-the-fold media, optimized fonts, caching and CDN delivery.

Avoid unnecessary third-party scripts and large animation libraries. Monitor real-user performance after launch.

Performance is especially important for Nigerian mobile users on variable networks.