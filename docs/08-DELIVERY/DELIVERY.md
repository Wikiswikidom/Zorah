# Delivery

Launch fulfilment is delivery only. Delivery rules must be configurable rather than embedded in frontend code.

A delivery quote is calculated from destination data and active rules. The selected delivery amount is captured in the order snapshot so historical totals remain stable.

Admin needs visibility into delivery status, destination, contact details and delivery instructions while respecting least-privilege access.

Carrier integration may be manual at launch and automated later.