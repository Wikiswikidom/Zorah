# Content Management

Content entries should support draft, published and archived states with author, timestamps and optional scheduled publication.

Editors need previews before publication. Media references must point to validated storage assets. Deleting content should prefer archival where historical references matter.

Content fields should have sensible length limits and structured formats rather than large free-form blobs when the UI needs predictable rendering.

SEO title/description and social metadata should be managed alongside relevant content.