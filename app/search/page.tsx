import {ShopControls,type StoreProduct} from "@/components/shop-controls";
import {createClient} from "@/lib/supabase/server";
import type {Metadata} from "next";
export const metadata:Metadata={title:"Search handbags | Zorah Handbags",description:"Search the live Zorah handbag catalogue."};
function toneFor(name:string):StoreProduct["tone"]{const n=name.toLowerCase();if(n.includes("green"))return"green";if(n.includes("brown")||n.includes("tan"))return"brown";if(n.includes("black"))return"black";return"ivory"}
export default async function SearchPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const p=await searchParams;const q=typeof p.q==="string"?p.q.trim().slice(0,80):"";const s=await createClient();const{data:rows}=await s.from("products").select("id,slug,name,base_price,currency,badge,is_featured,created_at,category:categories(name),variants:product_variants(color_name,stock_quantity,is_available,price),images:product_images(storage_path,alt_text,is_primary,sort_order)").eq("status","published").order("is_featured",{ascending:false}).order("created_at",{ascending:false}).limit(100);const products:StoreProduct[]=await Promise.all((rows??[]).map(async p=>{const variants=(p.variants??[]) as Array<{color_name:string|null;stock_quantity:number;is_available:boolean;price:number|null}>;const images=(p.images??[]) as Array<{storage_path:string;alt_text:string;is_primary:boolean;sort_order:number}>;const primary=[...images].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.sort_order-b.sort_order)[0];const signed=primary?await s.storage.from("product-media").createSignedUrl(primary.storage_path,3600):null;const colors=[...new Set(variants.map(v=>v.color_name).filter((v):v is string=>!!v))];const category=Array.isArray(p.category)?p.category[0]?.name??"Handbags":(p.category as {name?:string}|null)?.name??"Handbags";const price=variants.find(v=>v.price!=null)?.price??Number(p.base_price);const available=variants.some(v=>v.is_available&&v.stock_quantity>0)||variants.length===0;return{id:p.id,slug:p.slug,name:p.name,price:`${p.currency} ${price.toLocaleString("en-NG")}`,priceValue:price,category,colors:colors.length?colors:["Zorah"],availability:available?"In stock":"Made to order",tone:toneFor(colors[0]??category),imageUrl:signed?.data?.signedUrl??null,badge:p.badge,featuredRank:p.is_featured?0:1}}));return <main className="page-shell shop-page"><div className="page-intro"><p className="eyebrow">Zorah / Search</p><h1 className="page-title">Find your piece</h1><p className="page-lede">Search the live catalogue by handbag name, category or colour.</p></div><ShopControls products={products} initialQuery={q}/></main>}
