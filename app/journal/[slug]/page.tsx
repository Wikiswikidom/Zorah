import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { absoluteUrl, safeJsonLd } from '@/lib/seo'

export default async function StoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const supabase=await createClient();const {data:story}=await supabase.from('journal_articles').select('title,excerpt,body,category,published_at,seo_title,seo_description').eq('slug',slug).eq('status','published').single();if(!story)notFound();const article={'@context':'https://schema.org','@type':'Article','headline':story.title,'description':story.seo_description||story.excerpt||'Zorah Journal story on leather, design and Lagos.','datePublished':story.published_at||undefined,'author':{'@type':'Organization','name':'Zorah Handbags','url':absoluteUrl('/landing')},'publisher':{'@type':'Organization','name':'Zorah Handbags','url':absoluteUrl('/landing')},'mainEntityOfPage':absoluteUrl(`/journal/${slug}`)};return <main className="page-shell"><article className="mx-auto max-w-3xl"><p className="eyebrow">{story.category||"Z' Stories"} · {story.published_at?new Date(story.published_at).getFullYear():''}</p><h1 className="page-title">{story.title}</h1>{story.excerpt&&<p className="page-lede">{story.excerpt}</p>}<div className="mt-10 whitespace-pre-wrap text-base leading-8 text-black/75">{story.body}</div></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:safeJsonLd(article)}}/></main>}

export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const supabase=await createClient();const {data:story}=await supabase.from('journal_articles').select('title,excerpt,seo_title,seo_description').eq('slug',slug).eq('status','published').single();const title=story?.seo_title||story?.title||'Zorah Journal';const description=story?.seo_description||story?.excerpt||'Stories from Zorah about leather, design, craftsmanship and Lagos.';return {title,description,alternates:{canonical:`/journal/${slug}`},openGraph:{title,description,type:'article',url:absoluteUrl(`/journal/${slug}`)}}}
