'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
export default function InventoryAdjuster({variantId}:{variantId:string}){const router=useRouter();const [delta,setDelta]=useState(1);const [reason,setReason]=useState('restock');const [notes,setNotes]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');async function submit(){setBusy(true);setMessage('');try{const r=await fetch('/api/admin/inventory/movement',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({variant_id:variantId,quantity_delta:delta,reason,notes})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not update inventory.');setMessage('Inventory updated.');setNotes('');router.refresh()}catch(e){setMessage(e instanceof Error?e.message:'Could not update inventory.')}finally{setBusy(false)}}return <div className="mt-5 border-t border-black/10 pt-4"><div className="grid gap-3 sm:grid-cols-[120px_170px_1fr_auto]"><label className="text-xs uppercase tracking-[.1em]">Quantity<input type="number" min="-100000" max="100000" value={delta} onChange={e=>setDelta(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-black/15 bg-white px-3 py-2 text-sm"/></label><label className="text-xs uppercase tracking-[.1em]">Reason<select value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 w-full rounded-xl border border-black/15 bg-white px-3 py-2 text-sm"><option value="restock">Restock</option><option value="sale">Sale</option><option value="adjustment">Adjustment</option><option value="return">Return</option><option value="damage">Damage</option><option value="correction">Correction</option></select></label><label className="text-xs uppercase tracking-[.1em]">Notes<input maxLength={500} value={notes} onChange={e=>setNotes(e.target.value)} className="mt-2 w-full rounded-xl border border-black/15 bg-white px-3 py-2 text-sm"/></label><button disabled={busy||delta===0||!Number.isInteger(delta)} onClick={submit} className="rounded-full bg-[#173D32] px-5 py-2 text-xs font-medium uppercase tracking-[.12em] text-[#F7F3EC] disabled:opacity-50">{busy?'Saving…':'Adjust stock'}</button></div>{message&&<p className="mt-3 text-xs text-black/60">{message}</p>}</div>}
