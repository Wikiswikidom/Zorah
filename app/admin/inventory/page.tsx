import Link from 'next/link'
import { requireRole } from '@/lib/auth/authorization'
import { createClient } from '@/lib/supabase/server'
import InventoryAdjuster from './inventory-adjuster'
export default async function InventoryPage(){await requireRole(['catalog_admin']);const supabase=await createClient();const {data:variants,error}=await supabase.from('product_variants').select('id,sku,name,color_name,stock_quantity,is_available,product_id,products!inner(id,name,slug)').order('stock_quantity').limit(500);return <main className="min-h-screen bg-[#F7F3EC] text-[#111111]"><header className="border-b border-black/10 bg-[#111111] text-[#F7F3EC]"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8"><div><Link href="/admin" className="font-serif text-2xl tracking-[.12em]">ZORAH</Link><p className="mt-1 text-[10px] uppercase tracking-[.25em] text-[#B08A3C]">Inventory</p></div><Link href="/admin/products" className="text-xs uppercase tracking-[.18em] text-white/75">Products</Link></div></header><section className="mx-auto max-w-7xl px-5 py-10 sm:px-8 sm:py-14"><p className="text-xs uppercase tracking-[.2em] text-[#5A3524]">2D-4</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">Inventory</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-black/65">Track stock at variant level. Every adjustment is recorded as an inventory movement and cannot drive stock below zero.</p>{error?<div className="mt-8 rounded-2xl border border-red-200 bg-red-50 p-6 text-sm text-red-800">Inventory could not be loaded.</div>:<div className="mt-8 grid gap-4">{variants?.map(v=>{const product=Array.isArray(v.products)?v.products[0]:v.products;return <article key={v.id} className="rounded-2xl border border-black/10 bg-white/75 p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><p className="font-serif text-xl">{product?.name}</p><p className="mt-1 text-xs text-black/50">{v.sku}{v.name?` · ${v.name}`:''}{v.color_name?` · ${v.color_name}`:''}</p></div><div className="text-sm"><span className="text-black/50">Stock </span><strong>{v.stock_quantity}</strong></div><span className={`text-xs uppercase tracking-[.12em] ${v.is_available?'text-[#173D32]':'text-black/40'}`}>{v.is_available?'Available':'Unavailable'}</span></div><InventoryAdjuster variantId={v.id}/></article>})}</div>}</section></main>}
