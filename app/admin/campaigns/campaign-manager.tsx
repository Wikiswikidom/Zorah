'use client'

import { FormEvent, useEffect, useState } from 'react'

type Campaign = { id:string; name:string; slug:string; campaign_type:string; status:string; title:string; message:string|null; placement:string; priority:number; starts_at:string|null; ends_at:string|null; show_countdown:boolean; discount_type:string|null; discount_value:number|null }

const initial = { name:'', slug:'', campaign_type:'banner', status:'draft', title:'', message:'', cta_label:'Shop now', cta_href:'/shop', placement:'landing', priority:'0', starts_at:'', ends_at:'', show_countdown:false, discount_type:'none', discount_value:'' }

export default function CampaignManager(){
  const [items,setItems]=useState<Campaign[]>([]), [form,setForm]=useState(initial), [editing,setEditing]=useState<string|null>(null), [busy,setBusy]=useState(false), [error,setError]=useState(''), [notice,setNotice]=useState('')
  const load=async()=>{const r=await fetch('/api/admin/campaigns',{cache:'no-store'});const j=await r.json();if(!r.ok) throw new Error(j.error||'Could not load campaigns');setItems(j.campaigns||[])}
  useEffect(()=>{load().catch(e=>setError(e.message))},[])
  const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');setNotice('');try{const payload={...form,priority:Number(form.priority),discount_value:form.discount_value===''?null:Number(form.discount_value),starts_at:form.starts_at||null,ends_at:form.ends_at||null};const r=await fetch(editing?`/api/admin/campaigns/${editing}`:'/api/admin/campaigns',{method:editing?'PUT':'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok) throw new Error(j.error||'Could not save campaign');setForm(initial);setEditing(null);await load();setNotice('Campaign saved.')}catch(e){setError(e instanceof Error?e.message:'Could not save campaign')}finally{setBusy(false)}}
  const edit=(c:Campaign)=>setForm({name:c.name,slug:c.slug,campaign_type:c.campaign_type,status:c.status,title:c.title,message:c.message||'',cta_label:'Shop now',cta_href:'/shop',placement:c.placement,priority:String(c.priority),starts_at:c.starts_at?c.starts_at.slice(0,16):'',ends_at:c.ends_at?c.ends_at.slice(0,16):'',show_countdown:c.show_countdown,discount_type:c.discount_type||'none',discount_value:c.discount_value==null?'':String(c.discount_value)})
  const remove=async(id:string)=>{if(!confirm('Delete this campaign?'))return;setBusy(true);try{const r=await fetch(`/api/admin/campaigns/${id}`,{method:'DELETE'});if(!r.ok)throw new Error('Could not delete campaign');await load()}catch(e){setError(e instanceof Error?e.message:'Could not delete campaign')}finally{setBusy(false)}}
  const field=(key:keyof typeof form,label:string)=><label className="block text-xs uppercase tracking-[.12em]">{label}<input value={String(form[key])} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-2 w-full rounded-none border border-black/15 bg-white px-3 py-3 text-sm normal-case tracking-normal outline-none focus:border-[#B08A3C]" /></label>
  return <div className="grid gap-8 lg:grid-cols-[.9fr_1.1fr]">{<form onSubmit={submit} className="space-y-5 rounded-sm border border-black/10 bg-white p-5 sm:p-7">}<div className="flex items-center justify-between"><h2 className="font-serif text-2xl">{editing?'Edit campaign':'New campaign'}</h2>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(initial)}} className="text-xs uppercase tracking-[.12em]">Cancel</button>}</div>{field('name','Campaign name')}{field('slug','Slug')}<div className="grid gap-4 sm:grid-cols-2">{field('title','Public title')}{field('priority','Priority')}</div><label className="block text-xs uppercase tracking-[.12em]">Type<select value={form.campaign_type} onChange={e=>setForm({...form,campaign_type:e.target.value})} className="mt-2 w-full border border-black/15 bg-white px-3 py-3 text-sm normal-case tracking-normal"><option value="announcement">Announcement</option><option value="hero">Hero</option><option value="banner">Banner</option><option value="flash_sale">Flash sale</option><option value="product_promotion">Product promotion</option><option value="collection_promotion">Collection promotion</option><option value="editorial">Editorial</option></select></label>{field('message','Message')}<div className="grid gap-4 sm:grid-cols-2"><label className="block text-xs uppercase tracking-[.12em]">Placement<select value={form.placement} onChange={e=>setForm({...form,placement:e.target.value})} className="mt-2 w-full border border-black/15 bg-white px-3 py-3 text-sm normal-case tracking-normal"><option value="landing">Landing</option><option value="shop">Shop</option><option value="both">Both</option></select></label><label className="block text-xs uppercase tracking-[.12em]">Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})} className="mt-2 w-full border border-black/15 bg-white px-3 py-3 text-sm normal-case tracking-normal"><option value="draft">Draft</option><option value="scheduled">Scheduled</option><option value="live">Live</option><option value="paused">Paused</option><option value="expired">Expired</option><option value="archived">Archived</option></select></label></div><div className="grid gap-4 sm:grid-cols-2">{field('starts_at','Starts')}{field('ends_at','Ends')}</div><div className="grid gap-4 sm:grid-cols-2"><label className="block text-xs uppercase tracking-[.12em]">Discount<select value={form.discount_type} onChange={e=>setForm({...form,discount_type:e.target.value})} className="mt-2 w-full border border-black/15 bg-white px-3 py-3 text-sm normal-case tracking-normal"><option value="none">None</option><option value="percentage">Percentage</option><option value="fixed_amount">Fixed amount</option></select></label>{field('discount_value','Discount value')}</div><label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={form.show_countdown} onChange={e=>setForm({...form,show_countdown:e.target.checked})}/>Show countdown when an end time exists</label><div className="flex gap-3"><button disabled={busy} className="bg-[#111111] px-5 py-3 text-xs uppercase tracking-[.16em] text-white disabled:opacity-50">{busy?'Saving…':editing?'Save changes':'Create campaign'}</button></div>{error&&<p className="text-sm text-red-700">{error}</p>}{notice&&<p className="text-sm text-[#173D32]">{notice}</p>}</form>}
  <section className="space-y-3">{items.length===0?<div className="border border-dashed border-black/15 p-8 text-sm text-black/55">No campaigns yet.</div>:items.map(c=><article key={c.id} className="border border-black/10 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] uppercase tracking-[.16em] text-[#5A3524]">{c.campaign_type} · {c.placement}</p><h3 className="mt-1 font-serif text-xl">{c.title}</h3><p className="mt-1 text-xs text-black/50">{c.status} · priority {c.priority}</p></div><div className="flex gap-3 text-xs uppercase tracking-[.12em]"><button onClick={()=>{setEditing(c.id);edit(c)}}>Edit</button><button onClick={()=>remove(c.id)} className="text-red-700">Delete</button></div></div>{c.message&&<p className="mt-4 text-sm leading-6 text-black/65">{c.message}</p>}<div className="mt-4 text-xs text-black/50">{c.starts_at||'No start'} → {c.ends_at||'No end'}{c.show_countdown?' · Countdown':''}</div></article>)}</section></div>
}
