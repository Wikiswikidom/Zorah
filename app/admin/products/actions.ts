'use server'
import {revalidatePath} from 'next/cache';import {redirect} from 'next/navigation';import {requireRole} from '@/lib/auth/authorization';import {createClient} from '@/lib/supabase/server';
export async function deleteProduct(formData:FormData){await requireRole(['catalog_admin']);const id=String(formData.get('id')??'');if(!/^[0-9a-f-]{36}$/i.test(id))redirect('/admin/products?error=invalid');const s=await createClient();const{data:images}=await s.from('product_images').select('storage_path').eq('product_id',id);const{error}=await s.from('products').delete().eq('id',id);if(error)redirect('/admin/products?error=delete_failed');if(images?.length)await s.storage.from('product-media').remove(images.map(i=>i.storage_path));revalidatePath('/shop');revalidatePath('/admin/products');redirect('/admin/products?message=deleted')}
