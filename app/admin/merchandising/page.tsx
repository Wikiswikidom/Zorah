import Link from 'next/link'
import { requireRole } from '@/lib/auth/authorization'
import { createClient } from '@/lib/supabase/server'

export default async function AdminMerchandisingPage(){await requireRole(['catalog_admin','marketing_admin','super_admin']);const supabase=await createClient();const [{data:slots},{data:products}]=await Promise.all([supabase.from('merchandising_slots').select('id,slot_key,title,sort_order,is_enabled,starts_at,ends_at,product_id,products(name,slug)').order('slot_key').order('sort_order'),supabase.from('products').select('id,name,slug').eq('status','published').order('name')]);return <main className="min-h-screen bg-[#F7F3EC] text-[#111111]"><header className="border-b border-black/10 bg-[#111111] text-[#F7F3EC]"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8"><div><Link href="/admin" className="font-serif text-2xl tracking-[.12em]">ZORAH</Link><p className="mt-1 text-[10px] uppercase tracking-[.25em] text-[#B08A3C]">Merchandising studio</p></div><Link href="/" className="text-xs uppercase tracking-[.18em] text-white/75">View store</Link></div></header><section className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><p className="text-xs uppercase tracking-[.2em] text-[#5A3524]">2D-8</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">Merchandising</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-black/65">Control curated product placement without changing code. Availability windows prevent stale or unavailable products from being promoted.</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-[#111111] p-6 text-white"><p className="text-xs uppercase tracking-[.15em] text-[#B08A3C]">Published products</p><p className="mt-2 font-serif text-4xl">{products?.length??0}</p></div><div className="rounded-2xl bg-[#173D32] p-6 text-white"><p className="text-xs uppercase tracking-[.15em] text-white/60">Placements</p><p className="mt-2 font-serif text-4xl">{slots?.length??0}</p></div><div className="rounded-2xl bg-[#5A3524] p-6 text-white"><p className="text-xs uppercase tracking-[.15em] text-white/60">Strategy</p><p className="mt-2 font-serif text-2xl">Curated, not crowded.</p></div></div><div className="mt-8 overflow-hidden rounded-2xl border border-black/10 bg-white"><div className="grid grid-cols-[1fr_auto] border-b border-black/10 px-5 py-4 text-[10px] uppercase tracking-[.16em] text-black/45"><span>Placement / product</span><span>Order</span></div>{(slots??[]).map((s:any)=><div key={s.id} className="grid grid-cols-[1fr_auto] items-center gap-4 border-b border-black/10 px-5 py-5"><div><p className="text-[10px] uppercase tracking-[.15em] text-[#5A3524]">{s.slot_key} · {s.is_enabled?'Enabled':'Disabled'}</p><h2 className="mt-1 font-serif text-xl">{s.title||s.products?.name||'Untitled placement'}</h2><p className="text-xs text-black/45">{s.products?.slug||'Unknown product'}</p></div><span className="font-serif text-xl">{s.sort_order}</span></div>)}{!(slots?.length)&&<div className="px-5 py-12 text-sm text-black/55">No placements yet. Product merchandising controls will be expanded here as the catalogue grows.</div>}</div></section></main>}
