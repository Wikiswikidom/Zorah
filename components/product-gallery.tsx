"use client"
import {useState} from 'react'
import type {Product} from '@/lib/catalog'
type GalleryImage={url:string;alt:string}
export function ProductGallery({product,images=[]}:{product:Product;images?:GalleryImage[]}){const[active,setActive]=useState(0);const items=images.length?images:[];return <div className={`product-gallery tone-${product.tone}`}><div className="gallery-main">{items[active]?<img src={items[active].url} alt={items[active].alt||product.name} className="h-full w-full object-cover"/>:<span>{product.name}</span>}</div>{items.length>1&&<div className="gallery-thumbs" role="tablist" aria-label={`${product.name} images`}>{items.map((image,i)=><button type="button" key={`${image.url}-${i}`} className={active===i?'gallery-thumb is-active':'gallery-thumb'} onClick={()=>setActive(i)} style={{backgroundImage:`url(${image.url})`,backgroundSize:'cover',backgroundPosition:'center'}} role="tab" aria-selected={active===i} aria-label={`View image ${i+1}`}/>)}</div>}{items.length>0&&<p className="gallery-count">{active+1} / {items.length}</p>}{items.length===0&&<p className="gallery-note">Product photography will appear here once images are uploaded.</p>}</div>}
