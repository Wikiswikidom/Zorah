"use client";
import {useEffect,useState} from "react";
const empty={section_key:"",section_type:"hero",title:"",body:"",primary_cta_label:"Shop handbags",primary_cta_href:"/shop",secondary_cta_label:"",secondary_cta_href:"",media_path:"",theme:"dark",is_enabled:true,sort_order:10,status:"draft"};
export default function LandingCms(){const[rows,setRows]=useState<any[]>([]),[form,setForm]=useState<any>(empty),[id,setId]=useState<string|null>(null),[file,setFile]=useState<File|null>(null),[msg,setMsg]=useState("");const load=async()=>{const r=await fetch("/api/admin/content",{cache:"no-store"}),d=await r.json();if(r.ok)setRows(d.sections??[]);else setMsg(d.error??"Could not load content")};useEffect(()=>{load()},[]);const set=(k:string,v:any)=>setForm((x:any)=>({...x,[k]:v}));async function save(e:React.FormEvent){e.preventDefault();const r=await fetch(id?`/api/admin/content/${id}`:"/api/admin/content",{method:id?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,sort_order:Number(form.sort_order)})});const d=await r.json();setMsg(r.ok?"Saved.":d.error??"Save failed");if(r.ok){setId(null);setForm(empty);load()}}async function upload(){if(!file)return;const f=new FormData();f.append("file",file);const r=await fetch("/api/admin/content/media",{method:"POST",body:f});const d=await r.json();setMsg(r.ok?"Image uploaded — save the section to attach it.":d.error??"Upload failed");if(r.ok)set("media_path",d.path)}async function remove(x:string){if(!confirm("Remove this landing section?"))return;await fetch(`/api/admin/content/${x}`,{method:"DELETE"});load()}return <div className="grid gap-6 lg:grid-cols-[460px_1fr]"><form onSubmit={save} className="rounded-2xl border border-black/10 bg-white p-5 shadow-sm"><div className="mb-5 flex items-start justify-between"><div><h2 className="font-serif text-2xl">{id?"Edit section":"Add section"}</h2><p className="mt-1 text-xs text-black/55">Control text, images, buttons and visibility.</p></div>{id&&<button type="button" onClick={()=>{setId(null);setForm(empty)}}>Cancel</button>}</div>{msg&&<p className="mb-4 rounded-lg bg-[#EEF4F0] p-3 text-xs">{msg}</p>}<label className="mb-3 block text-xs font-semibold">Section key<input required className="mt-1 w-full rounded-lg border p-3" value={form.section_key} onChange={e=>set("section_key",e.target.value)} placeholder="hero-01 or site-logo"/></label><div className="grid grid-cols-2 gap-3"><label className="text-xs font-semibold">Type<select className="mt-1 w-full rounded-lg border p-3" value={form.section_type} onChange={e=>set("section_type",e.target.value)}>{["hero","editorial","craft","collections","custom_order","media","promo","journal","testimonial","newsletter"].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-xs font-semibold">Order<input className="mt-1 w-full rounded-lg border p-3" type="number" value={form.sort_order} onChange={e=>set("sort_order",e.target.value)}/></label></div><label className="my-3 block text-xs font-semibold">Title<input className="mt-1 w-full rounded-lg border p-3" value={form.title??""} onChange={e=>set("title",e.target.value)}/></label><label className="mb-3 block text-xs font-semibold">Text<textarea className="mt-1 w-full rounded-lg border p-3" rows={6} value={form.body??""} onChange={e=>set("body",e.target.value)}/></label><div className="grid grid-cols-2 gap-3"><label className="text-xs font-semibold">Button<input className="mt-1 w-full rounded-lg border p-3" value={form.primary_cta_label??""} onChange={e=>set("primary_cta_label",e.target.value)}/></label><label className="text-xs font-semibold">Link<input className="mt-1 w-full rounded-lg border p-3" value={form.primary_cta_href??""} onChange={e=>set("primary_cta_href",e.target.value)}/></label></div><label className="my-3 block text-xs font-semibold">Image<input className="mt-1 w-full rounded-lg border p-3" type="file" accept="image/jpeg,image/png,image/webp,image/avif" onChange={e=>setFile(e.target.files?.[0]??null)}/></label>{file&&<button type="button" className="mb-3 rounded-full border px-4 py-2 text-xs" onClick={upload}>Upload image</button>}{form.media_path&&<p className="mb-3 break-all text-[10px] text-black/45">Image attached</p>}<label className="mb-4 flex items-center gap-2 text-xs"><input type="checkbox" checked={!!form.is_enabled} onChange={e=>set("is_enabled",e.target.checked)}/> Visible</label><button className="w-full rounded-full bg-[#173D32] px-5 py-3 text-xs font-bold uppercase tracking-wider text-white">{id?"Save changes":"Add section"}</button></form><section className="rounded-2xl border border-black/10 bg-white p-5 shadow-sm"><div className="mb-4 flex items-end justify-between"><div><h2 className="font-serif text-2xl">Landing controls</h2><p className="mt-1 text-xs text-black/55">Hero slides: {rows.filter(x=>x.section_type==="hero").length}/5</p></div><button onClick={load} className="text-xs underline">Refresh</button></div><div className="mb-4 rounded-lg bg-[#F5F1E9] p-3 text-xs"><b>Logo:</b> edit the <b>site-logo</b> section and upload the replacement.</div>{rows.map(x=><div key={x.id} className="flex items-center justify-between gap-4 border-b py-4 last:border-0"><div className="min-w-0"><h3 className="font-serif text-lg">{x.title||x.section_key}</h3><p className="text-[10px] uppercase tracking-wider text-black/45">{x.section_type} · {x.status} · order {x.sort_order}</p></div><div className="flex shrink-0 gap-2"><button className="rounded-full border px-3 py-2 text-[10px] uppercase" onClick={()=>{setId(x.id);setForm(x)}}>Edit</button><button className="rounded-full border border-red-200 px-3 py-2 text-[10px] uppercase text-red-700" onClick={()=>remove(x.id)}>Remove</button></div></div>)}</section></div>}
