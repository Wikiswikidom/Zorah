import { EditorialMotion } from "./editorial-motion";
import { ActiveCampaigns } from "@/components/active-campaigns";
import { createAdminClient } from "@/lib/supabase/admin";
import { createClient } from "@/lib/supabase/server";

type Section = { id:string; section_key:string; section_type:string; eyebrow:string|null; title:string|null; body:string|null; primary_cta_label:string|null; primary_cta_href:string|null; secondary_cta_label:string|null; secondary_cta_href:string|null; media_path:string|null; theme:string; is_enabled:boolean; sort_order:number; status:string; scheduled_publish_at:string|null };
type Campaign = { id:string; title:string; message:string|null; cta_label:string|null; cta_href:string|null; campaign_type:string; media_path:string|null; ends_at:string|null; show_countdown:boolean; discount_type:string|null; discount_value:number|null };
type HomeProduct = { id:string; slug:string; name:string; price:string; priceValue:number; category:string; colors:string[]; availability:string; tone:"brown"|"green"|"ivory"|"black"; imageUrl:string|null; badge:string|null };
type DbClient = ReturnType<typeof createAdminClient>;
function toneFor(value:string):HomeProduct["tone"]{const v=value.toLowerCase();if(v.includes("green"))return "green";if(v.includes("brown")||v.includes("tan"))return "brown";if(v.includes("black"))return "black";return "ivory"}
async function getPublicStorefrontClient():Promise<{client:DbClient|null;signedMedia:boolean}>{try{return{client:createAdminClient(),signedMedia:true}}catch(error){console.error("Storefront admin client unavailable; using public Supabase client.",error);try{return{client:(await createClient()) as unknown as DbClient,signedMedia:false}}catch(publicError){console.error("Storefront Supabase client unavailable; using static storefront fallback.",publicError);return{client:null,signedMedia:false}}}}
export default async function StorefrontHome(){const{client:supabase,signedMedia}=await getPublicStorefrontClient();const now=new Date();if(!supabase)return<><ActiveCampaigns campaigns={[]}/><EditorialMotion sections={[]} logoUrl="/brand/zorah-logo.webp" products={[]}/></>;try{const[sectionsResult,campaignsResult,productsResult,brandingResult]=await Promise.all([supabase.from("landing_sections").select("id,section_key,section_type,eyebrow,title,body,primary_cta_label,primary_cta_href,secondary_cta_label,secondary_cta_href,media_path,theme,is_enabled,sort_order,status,scheduled_publish_at").eq("status","published").eq("is_enabled",true).order("sort_order",{ascending:true}).order("created_at",{ascending:true}),supabase.from("campaigns").select("id,title,message,cta_label,cta_href,campaign_type,media_path,ends_at,show_countdown,discount_type,discount_value,starts_at").eq("status","live").in("placement",["landing","both"]).order("priority",{ascending:false}).limit(10),supabase.from("products").select("id,slug,name,base_price,currency,badge,is_featured,category:categories(name),variants:product_variants(color_name,stock_quantity,is_available,price),images:product_images(storage_path,alt_text,is_primary,sort_order)").eq("status","published").order("is_featured",{ascending:false}).order("created_at",{ascending:false}).limit(8),supabase.from("site_settings").select("key,media_path").in("key",["site_logo"]).maybeSingle()]);if(sectionsResult.error)console.error("Storefront landing sections query failed",sectionsResult.error);if(campaignsResult.error)console.error("Storefront campaigns query failed",campaignsResult.error);if(productsResult.error)console.error("Storefront products query failed",productsResult.error);if(brandingResult.error)console.error("Storefront branding query failed",brandingResult.error);const sections=((sectionsResult.data??[]) as Section[]).filter(s=>!s.scheduled_publish_at||new Date(s.scheduled_publish_at).getTime()<=now.getTime());const sectionUrl=(path:string|null)=>{if(!path)return null;if(/^https?:\/\//i.test(path)||path.startsWith("/"))return path;return supabase.storage.from("landing-media").getPublicUrl(path).data.publicUrl||null};const media=Object.fromEntries(sections.map(s=>[s.section_key,sectionUrl(s.media_path)]));const configuredLogo=brandingResult.data?.media_path?supabase.storage.from("brand-assets").getPublicUrl(brandingResult.data.media_path).data.publicUrl:null;const logo=configuredLogo||media["site-logo"]||"/brand/zorah-logo.webp";const signedUrl=async(path:string|null,expires:number)=>{if(!path)return null;if(/^https?:\/\//i.test(path)||path.startsWith("/"))return path;if(!signedMedia)return null;try{return(await supabase.storage.from("product-media").createSignedUrl(path,expires)).data?.signedUrl??null}catch(error){console.error("Storefront media URL generation failed",error);return null}};const activeCampaigns:Campaign[]=await Promise.all(((campaignsResult.data??[]) as Array<Campaign&{starts_at:string|null}>).filter(c=>{const starts=c.starts_at?new Date(c.starts_at).getTime():-Infinity;const ends=c.ends_at?new Date(c.ends_at).getTime():Infinity;return starts<=now.getTime()&&ends>now.getTime()}).map(async c=>({...c,media_path:await signedUrl(c.media_path,900)})));const homeProducts:HomeProduct[]=await Promise.all(((productsResult.data??[]) as any[]).map(async p=>{const variants=(p.variants??[]) as Array<{color_name:string|null;stock_quantity:number;is_available:boolean;price:number|null}>;const images=(p.images??[]) as Array<{storage_path:string;alt_text:string;is_primary:boolean;sort_order:number}>;const primary=[...images].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.sort_order-b.sort_order)[0];const category=Array.isArray(p.category)?p.category[0]?.name??"Handbags":(p.category as {name?:string}|null)?.name??"Handbags";const colors=[...new Set(variants.map(v=>v.color_name).filter((v):v is string=>!!v))];const price=variants.find(v=>v.price!=null)?.price??Number(p.base_price);const available=variants.some(v=>v.is_available&&v.stock_quantity>0)||variants.length===0;return{id:p.id,slug:p.slug,name:p.name,price:`${p.currency} ${price.toLocaleString("en-NG")}`,priceValue:price,category,colors:colors.length?colors:["Default"],availability:available?"In stock":"Made to order",tone:toneFor(colors[0]??category),imageUrl:await signedUrl(primary?.storage_path??null,3600),badge:p.badge??null}}));return<><ActiveCampaigns campaigns={activeCampaigns}/><EditorialMotion sections={sections.map(s=>({...s,media_url:media[s.section_key]??null}))} logoUrl={logo} products={homeProducts}/></>}catch(error){console.error("Storefront homepage data load failed; rendering safe fallback.",error);return<><ActiveCampaigns campaigns={[]}/><EditorialMotion sections={[]} logoUrl="/brand/zorah-logo.webp" products={[]}/></>}}
