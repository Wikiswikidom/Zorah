"use client";
import { useCommerce } from "@/components/commerce-provider";
type ProductCardProps={slug?:string;name:string;price:string;priceValue?:number;tone?:"ivory"|"brown"|"green"|"black";imageUrl?:string|null;badge?:string|null;category?:string;availability?:string};
const tones={ivory:"#e6ded2",brown:"#6b4632",green:"#23483b",black:"#242424"};
export function ProductCard({slug,name,price,tone="ivory",imageUrl,badge,category,availability}:ProductCardProps){const{isWishlisted,toggleWishlist}=useCommerce();const saved=slug?isWishlisted(slug):false;return <article className="product-card"><div className="product-media-wrap"><a href={slug?`/products/${slug}`:"/shop"} aria-label={`View ${name}`}><div className="product-media">{imageUrl?<img src={imageUrl} alt="" className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.03]"/>:<div className="product-placeholder" style={{background:`linear-gradient(145deg, ${tones[tone]}, #f7f3ec22)`}} aria-hidden="true">Z</div>}{badge&&<span className="product-badge">{badge}</span>}</div></a>{slug&&<button className={`wishlist-button ${saved?"is-saved":""}`} onClick={()=>toggleWishlist(slug)} aria-label={saved?`Remove ${name} from wishlist`:`Save ${name} to wishlist`} aria-pressed={saved}>{saved?"♥":"♡"}</button>}</div><a href={slug?`/products/${slug}`:"/shop"} className="product-info"><div><p className="product-name">{name}</p>{category&&<p className="product-category">{category}</p>}</div><div className="text-right"><p className="product-price">{price}</p>{availability&&<p className="product-availability">{availability}</p>}</div></a></article>}
